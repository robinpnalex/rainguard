# Vercel installs Python dependencies from the repository root.
# Kept in sync with backend/requirements.txt, plus the Postgres driver.
fastapi==0.115.6
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
pillow==11.1.0
psycopg[binary]==3.2.3
