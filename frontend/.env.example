# Origin of the deployed NIRVANA API, with no trailing slash.
# Leave this unset for local development -- the Vite dev server proxies
# /api and /images to localhost:8000 instead.
#
# Set it in Vercel: Project -> Settings -> Environment Variables
VITE_API_BASE=https://rainguard-api-zzw6.onrender.com
